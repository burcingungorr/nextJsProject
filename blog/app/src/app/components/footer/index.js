import React from "react";
import styles from "./styles.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h4 className={styles.h4}>goodbye</h4>
    </footer>
  );
};

export default Footer;
